upstream telemost_bot {
    server 127.0.0.1:3010;
}

# HTTP -> HTTPS редирект
server {
    listen 80;
    server_name telemosts.ru www.telemosts.ru;
    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS сервер
server {
    listen 443 ssl;
    server_name telemosts.ru www.telemosts.ru;

    # SSL
    ssl_certificate     /etc/letsencrypt/live/telemosts.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/telemosts.ru/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    # Логи
    access_log /Users/lea_alex/servers/log/nginx/telemosts.ru.access.log;
    error_log  /Users/lea_alex/servers/log/nginx/telemosts.ru.error.log;

    location = /bot/telemost        { return 301 /bot/telemost/app/; }
    location = /bot/telemost/app    { return 301 /bot/telemost/app/; }

    location /bot/telemost/ {
        proxy_pass http://telemost_bot/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /bot/telemost/health {
        proxy_pass http://telemost_bot/health;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        access_log off;
    }
}